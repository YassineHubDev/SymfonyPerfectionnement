{% extends 'base.html.twig' %}

{% block body %}
    <h1>Page d'accueil</h1>

    {% include 'product/list.html.twig' with {products : paginateResults} %}

    <div class="navigation">
        {{ knp_pagination_render(paginateResults) }}
    </div>

{% endblock %}